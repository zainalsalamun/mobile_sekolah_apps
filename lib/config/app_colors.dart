import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY
  static const Color primary = Color(0xFF1E6FFB);
  static const Color primaryDark = Color(0xFF0E5AD6);
  static const Color primaryLight = Color(0xFFE8F1FF);

  // NEUTRAL
  static const Color background = Color(0xFFF5F6FA);
  static const Color white = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFF1C1C1E);
  static const Color textMedium = Color(0xFF636366);
  static const Color textLight = Color(0xFFAEAEB2);

  // STATUS
  static const Color success = Color(0xFF2ECC71);
  static const Color warning = Color(0xFFF5A623);
  static const Color info = Color(0xFF2E9AFE);
  static const Color error = Color(0xFFFF3B30);

  // BORDER & SHADOW
  static const Color border = Color(0xFFE5E5EA);
  static const Color shadow = Color.fromRGBO(0, 0, 0, 0.08);
}
